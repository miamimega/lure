server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location /llm/ {
        proxy_pass http://lure-llm:8788/;
        proxy_read_timeout 100s;
        proxy_set_header X-Forwarded-For $remote_addr;
    }
    location / { try_files $uri /index.html; }
}
